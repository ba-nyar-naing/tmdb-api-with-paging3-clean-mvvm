dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "tmdb-showcase"

include ':presentation'
include ':domain'
include ':data-local'
include ':data-remote'
